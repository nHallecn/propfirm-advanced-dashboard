export type Range = "7D" | "30D" | "90D" | "All";

export type AppDialog = {
  title: string;
  description: string;
  actionLabel?: string;
  detail?: string;
};

export type PageMeta = {
  eyebrow: string;
  title: string;
  subtitle: string;
};
